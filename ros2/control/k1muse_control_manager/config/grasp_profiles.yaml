robot_geometry:
  arm_mount_x: 0.030406
  arm_mount_y: 0.0
  arm_base_z_at_lift_zero: 0.222
  lift_min_z: 0.0
  lift_max_z: 0.45

gripper_limits:
  min_rad: -0.167203918
  max_rad: 1.88833046

objects:
  bottle:
    default_variant: side
    variants:
      side:
        mode: side
        gripper_strategy: fixed_angle
        calibrated: true
        gripper_open_rad: 1.30
        gripper_close_rad: 0.38
        roll_rad: 1.5707963
        pitch_rad: 0.0
        preferred_arm_tcp: [0.43, 0.0, 0.13]
        approach_distance_m: 0.08
        retreat_distance_m: 0.08
        lift_distance_m: 0.03
        carry_pose: [0.24, 0.0, 0.18, 1.5707963, 0.0]
        transport_max_v: 0.15
        transport_max_omega: 0.40
        settle_time_ms: 2000

  box:
    default_variant: side
    variants:
      side:
        mode: side
        gripper_strategy: fixed_angle
        calibrated: true
        gripper_open_rad: 1.30
        gripper_close_rad: 0.30
        roll_rad: 1.5707963
        pitch_rad: 0.0
        preferred_arm_tcp: [0.43, 0.0, 0.13]
        approach_distance_m: 0.08
        retreat_distance_m: 0.08
        lift_distance_m: 0.05
        carry_pose: [0.24, 0.0, 0.18, 1.5707963, 0.0]
        transport_max_v: 0.15
        transport_max_omega: 0.40
        settle_time_ms: 2000

      top:
        mode: top
        gripper_strategy: fixed_angle
        calibrated: true
        gripper_open_rad: 1.30
        gripper_close_rad: 0.30
        roll_rad: 0.0
        pitch_rad: 1.5
        preferred_arm_tcp: [0.30, 0.0, -0.039]
        approach_distance_m: 0.05
        retreat_distance_m: 0.03
        lift_distance_m: 0.03
        carry_pose: [0.24, 0.0, 0.08, 0.0, 1.5]
        transport_max_v: 0.15
        transport_max_omega: 0.40
        settle_time_ms: 2000

  umbrella:
    default_variant: top
    variants:
      top:
        mode: top
        gripper_strategy: fixed_angle
        calibrated: true
        gripper_open_rad: 1.30
        gripper_close_rad: 0.30
        roll_rad: 0.0
        pitch_rad: 1.5
        preferred_arm_tcp: [0.30, 0.0, -0.039]
        approach_distance_m: 0.05
        retreat_distance_m: 0.03
        lift_distance_m: 0.03
        carry_pose: [0.24, 0.0, 0.08, 0.0, 1.5]
        transport_max_v: 0.15
        transport_max_omega: 0.40
        settle_time_ms: 2000
